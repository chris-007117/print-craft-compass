
The user wants a production blog system on the existing Veridia Press site. The current `/insights` page is a static stub — I'll replace it with a Supabase-backed dynamic blog at `/blog` (keeping `/insights` as a redirect for the existing nav/sitemap link).

Let me check the current Insights page and routing to plan the swap cleanly.
